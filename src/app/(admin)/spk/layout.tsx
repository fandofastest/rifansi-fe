export const metadata = {
  title: "Spk Management | Admin Dashboard",
  description: "Manage Spk in the admin dashboard. Add, edit, and delete Spk easily.",
};

export default function UnitsLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
} 