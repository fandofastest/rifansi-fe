export const metadata = {
  title: "Categories Management | Admin Dashboard",
  description: "Manage Categories in the admin dashboard. Add, edit, and delete Categories easily.",
};

export default function UnitsLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
} 