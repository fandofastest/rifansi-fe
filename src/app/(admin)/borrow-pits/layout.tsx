export const metadata = {
  title: "Borrow Pits Management | Admin Dashboard",
  description:
    "Manage Borrow Pits in the admin dashboard. Add, edit, and delete borrow pits easily.",
};

export default function BorrowPitsLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
