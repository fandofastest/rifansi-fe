export const metadata = {
  title: "Unit Management | Admin Dashboard",
  description: "Manage units in the admin dashboard. Add, edit, and delete units easily.",
};

export default function UnitsLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
} 