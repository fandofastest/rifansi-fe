export const metadata = {
  title: "Areas Management | Admin Dashboard",
  description: "Manage Areas in the admin dashboard. Add, edit, and delete Areas easily.",
};

export default function UnitsLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
} 